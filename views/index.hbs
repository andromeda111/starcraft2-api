<h1>Database (current)</h1>

<div class="row">
  <div class="container col-md-8 col-md-offset-2 well">
    {{#if data}}
    {{data}}
    {{else}}
    No Database Information Available
    {{/if}}

  </div>

  <div class="container col-md-8 col-md-offset-2 well">
    <table id="datatable" class="table table-striped table-hover table-bordered" cellspacing="0" width="100%">
      <thead>
        <tr>
          <th>Name</th>
        </tr>
      </thead>

    </table>
  </div>
</div>
